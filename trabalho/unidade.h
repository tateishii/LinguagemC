#include<stdio.h>
#include<stdlib.h>

typedef struct unidade  //typedef é o tipo de arquivo, struct é uma estrutura para criar dados
{
    char unidade[100];
    char local[100];
    char horariofuncionamento[30];
    char telefone[30];

}unidade;